import RegisterAuth from "@/features/auth-components/register/register-auth.tsx";
import LayoutPages from "@/layout/layout-auth.tsx";

export default function RegisterAuthPages() {
    return (
        <LayoutPages>
            <RegisterAuth/>
        </LayoutPages>
    )
}